version: "3"

services:
  archiver-viewer:
    deploy:
      mode: replicated
      replicas: 1
    image: httpd:2.4.35
    ports:
      - "20090:80"
    volumes:
      - "/home/con-srv/repository/archiver-viewer:/usr/local/apache2/htdocs/archiver-viewer-beta"

